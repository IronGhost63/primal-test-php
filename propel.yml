propel:
    database:
        connections:
            default:
                adapter: sqlite
                dsn: sqlite:./database/database.sq3
                user: root
                password: password
                settings:
                    charset: utf8
    paths:
        phpDir: ./src/Models
        phpConfDir: ./config
        sqlDir: ./database
        migrationDir: ./database/migrations
    generator:
        namespaceAutoPackage: false
